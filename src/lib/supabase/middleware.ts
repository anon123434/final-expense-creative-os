/**
 * Supabase middleware helper — auth guards removed for single-user local mode.
 * File kept so auth can be re-added later.
 *
 * Previously this refreshed the Supabase session and redirected
 * unauthenticated users to /login.
 */
